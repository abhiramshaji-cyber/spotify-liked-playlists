{
  "name": "spotify-liked-playlists",
  "private": true,
  "type": "module",
  "scripts": {
    "auth": "bun run auth.ts",
    "sync": "bun run sync.ts",
    "dry": "bun run sync.ts --dry-run",
    "sync:genre": "bun run sync.ts --by=genre --source=musicbrainz --min=8",
    "test": "bun run genres.test.ts",
    "dry:genre": "bun run sync.ts --by=genre --source=musicbrainz --dry-run"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
